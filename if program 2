#include <stdio.h>
#include <string.h>

int main() {
    
    char name[50];
    printf("Enter your name: ");
    fgets(name, sizeof(name), stdin);

    if (strlen(name) > 10) {
        printf("Long name\n");
    } else {
        printf("Short name\n");
    }
    return 0;
}